# Functions are reusable blocks of code, enhancing modularity by enclosing functionality into separate, organized units.
# Avoid code duplication and significantly improves code readability.

def greet():
    print("Hello World!")

# Call the function to execute
greet()

def addition():
    a = int(input("Enter a number a: "))
    b = int(input("Enter another number b:"))
    print(a + b)

addition()